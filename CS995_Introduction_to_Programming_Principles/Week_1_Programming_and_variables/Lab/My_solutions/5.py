"""
Write a program that contains two integer variables, where the
first one contains 3 and the second one contains 2. Divide the
first variable by the second variable and assign the result to
a new variable. Check the type of the third variable by using
print(type(result)), where result contains the result of the
division
"""

int_1 = 3
int_2 = 2
result = int_1/int_2

print(type(result))
